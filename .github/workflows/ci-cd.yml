name: CI/CD Pipeline - Enterprise E-Commerce (Disabled)

# Pipeline disabled following system decommission
on:
  workflow_dispatch:

jobs:
  offline-notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "System decommissioned. All pipelines disabled."
